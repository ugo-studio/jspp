{
  "name": "jspp",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "precompile": "bun run scripts/precompile-headers.ts",
    "test": "bun run precompile && bun test",
    "code": "bun run scripts/run-js.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}